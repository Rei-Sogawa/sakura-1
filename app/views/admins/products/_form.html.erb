<%= form_with(model: admins_product) do |form| %>
  <% if admins_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_product.errors.count, "error") %> prohibited this admins_product from being saved:</h2>

      <ul>
        <% admins_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
